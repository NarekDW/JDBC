group 'JDBC'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compileOnly 'org.projectlombok:lombok:+'
    testCompile 'junit:junit:+', 'org.projectlombok:lombok:+'
    compile "com.h2database:h2:+"
}
